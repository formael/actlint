{
  "name": "@formael/actlint-eval",
  "version": "0.0.0",
  "private": true,
  "description": "IMPURE TOOLING (never published). The labeled eval corpus and its FP-weighted scorer — the quality number that defends the scorecard. Reads the corpus from disk, runs the pure core over it, and gates on precision regressions.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "eval": "tsx harness/run.ts"
  },
  "dependencies": {
    "@formael/action-risk-vocabulary": "workspace:*",
    "@formael/actlint-core": "workspace:*",
    "zod": "^4.4.3"
  }
}
