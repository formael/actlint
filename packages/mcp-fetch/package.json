{
  "name": "@formael/actlint-mcp-fetch",
  "version": "0.0.0",
  "private": true,
  "description": "IMPURE. The only network code and the only place the MCP SDK appears. Acquires a server's tools and returns a normalized ToolManifest behind an anti-corruption boundary. Depends on core ONLY via @formael/actlint-core/contracts.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@formael/actlint-core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
