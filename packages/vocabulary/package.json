{
  "name": "@formael/action-risk-vocabulary",
  "version": "0.0.0",
  "private": true,
  "description": "PURE DATA. The action-risk vocabulary and standards crosswalk, JSON-Schema'd and independently versioned. The single shared artifact between actlint (open) and the Formael platform — data with a schema, never code.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "clean": "rm -rf dist"
  }
}
