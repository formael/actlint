{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://formael.dev/schemas/action-risk-vocabulary/crosswalk.schema.json",
  "title": "actlint standards & regulatory crosswalk",
  "description": "Maps each stable RuleId to the external frameworks it is relevant to (OWASP ASI, OWASP MCP Top 10, CoSAI/OASIS, EU AI Act, NIST) and the MCP annotation field it concerns. Pure data: it carries no mechanism. Standards references are relevant, not prescriptive — a finding is never a compliance verdict. Every RuleId the engine can emit must have a non-empty entry here; that completeness is a contract test in packages/core.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "map"],
  "properties": {
    "$schema": { "type": "string" },
    "version": {
      "type": "string",
      "description": "Semver of the crosswalk dataset itself — an independent line from the engine and vocabulary versions.",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "note": {
      "type": "string",
      "minLength": 1,
      "description": "The 'relevant, not prescriptive' caveat, stated in the data so it travels with it."
    },
    "map": {
      "type": "object",
      "description": "RuleId -> the standards it is relevant to. Keys are stable, kebab-case RuleIds.",
      "minProperties": 1,
      "propertyNames": { "pattern": "^[a-z][a-z0-9-]*$" },
      "additionalProperties": { "$ref": "#/$defs/standardsRef" }
    }
  },
  "$defs": {
    "standardsRef": {
      "type": "object",
      "additionalProperties": false,
      "description": "At least one array must be present and non-empty — a finding that cannot locate itself in the standards landscape is unshippable.",
      "properties": {
        "owaspAsi": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^ASI(0[1-9]|10):2026$",
            "description": "OWASP Top 10 for Agentic Applications (2026) identifier, e.g. ASI02:2026."
          }
        },
        "owaspMcp": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^MCP(0[1-9]|10):2025$",
            "description": "OWASP MCP Top 10 (beta, 2025 edition) identifier, e.g. MCP02:2025. The year suffix pins which edition the claim was made against."
          }
        },
        "cosaiOasis": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^MCP-T([1-9]|1[0-2])$",
            "description": "CoSAI/OASIS MCP Security taxonomy (v1, Jan 2026) category identifier, e.g. MCP-T9."
          }
        },
        "euAiAct": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^Art\\.\\d{1,3}$",
            "description": "EU AI Act article reference, e.g. Art.14."
          }
        },
        "nist": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^AI-RMF:(GOVERN|MAP|MEASURE|MANAGE)$",
            "description": "NIST AI RMF function reference, e.g. AI-RMF:MEASURE."
          }
        },
        "mcpField": {
          "type": "array",
          "minItems": 1,
          "items": {
            "enum": ["readOnlyHint", "destructiveHint", "idempotentHint", "openWorldHint"],
            "description": "The MCP ToolAnnotations field this rule observes."
          }
        }
      },
      "anyOf": [
        { "required": ["owaspAsi"] },
        { "required": ["owaspMcp"] },
        { "required": ["cosaiOasis"] },
        { "required": ["euAiAct"] },
        { "required": ["nist"] },
        { "required": ["mcpField"] }
      ]
    }
  }
}
